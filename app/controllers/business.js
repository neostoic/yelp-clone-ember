import Ember from 'ember';

export default Ember.Controller.extend({
  // filteredReviews: function() {
  //   this.get('model').reload();
  //   return this.get('model').get('reviews').filterBy('id');
  // }.property('model.reviews.@each.id')
});
